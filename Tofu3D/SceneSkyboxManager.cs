namespace Tofu3D;

public class SceneSkyboxManager
{
	Scene _scene;
	SceneSkyboxRenderer _sceneSkyboxRenderer;

	public SceneSkyboxManager(Scene scene)
	{
		_scene = _scene;
		_sceneSkyboxRenderer = new SceneSkyboxRenderer(scene);
	}
}